import { FC } from "react";

import styles from "./Events.module.scss";

import Image from "next/image";

const Events: FC = () => {
    return (
        <section className={styles.eventsMain}>
            Events page will go here
        </section>
    );
};

export default Events;
