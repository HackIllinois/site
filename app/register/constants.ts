export const LATE_REGISTRATION_PASSWORD = "HACKY204oPA6fR26Cg";
/**
 * If true, on registration and challenge pages, users get redirected to /registration-closed. Only /register/late remains accessible.
 * If false, registration is closed only if /register/status indicates so.
 *
 * As of 2/4/2026, do NOT set this to false until the API re-closes registration.
 * */
export const FORCE_REGISTRATION_CLOSED = true;
